---
// RetakeSafe moved to its own home. This page existed for a few hours and may
// have been shared, so it redirects rather than 404s.
export const prerender = false;
return Astro.redirect('https://retakesafe.com/', 301);
---
